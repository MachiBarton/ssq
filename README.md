# 大乐透自助机选

一个纯静态的大乐透选号页面，基于 `Vite + React + shadcn/ui`。

现在只保留这些能力：

- 高级机选默认打开
- 单式、复式、胆拖选号
- 追加、多倍、金额计算
- 本地保存最近选号结果

已移除：

- 历史开奖下载
- 号码预测和分析
- 所有定时同步脚本
- 自动数据更新工作流

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 部署

项目基路径固定为 `/ssq`，适合发布到 GitHub Pages 项目站点。
